# Upstream dsh (deepseek-harness) commit pinned for the build-time vendor tree
# (vendor/harness-packages/@deepseek-ai) — read by scripts/ensure-harness-vendor.mjs.
# Keep in sync with pnpm-lock.yaml: bumping this without regenerating the lockfile
# makes `pnpm install --frozen-lockfile` fail (intended reproducibility guard).
# Per-run override: DSH_CHAMBER_HARNESS_COMMIT.
99f6f02fecdb7dff40c3fbc9470f5907c29f74ca
